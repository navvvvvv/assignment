import React from "react";

export const Home = () => {
  return (
    <h3 className="navbar bg-primary" data-bs-theme="dark" style={{height: 200}}>
      The picture below shows different HTML elements with its name that gives
      you a rough idea of how to create the HTML structure of the home page.
      Image Explanation: The home page is divided into 3 sections: header, main
      and, footer. The header section contains the logo and the navigation bar.
    </h3>
  );
};
